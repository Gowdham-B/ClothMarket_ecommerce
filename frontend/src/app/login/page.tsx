"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation"; import { useDemoAuth } from "@/state/useDemoAuth"; import styles from "../auth.module.css";
export default function LoginPage() { const router = useRouter(); const { signIn } = useDemoAuth(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!email.includes("@") || password.length < 6) { setError("Enter a valid email and a password of at least 6 characters."); return; } signIn({ name: email.split("@")[0], email }); router.push("/account"); } return <main className={`shell ${styles.authPage}`}><section className={styles.authIntro}><p className="eyebrow">WELCOME BACK</p><h1>Keep your best finds <em>close.</em></h1><p>Sign in to carry saved pieces and future price alerts across devices.</p></section><section className={styles.authCard}><h2>Sign in</h2><p className={styles.formNote}>Demo session only — no credentials are sent anywhere.</p><form onSubmit={submit} noValidate><label>Email address<input type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={(event) => setPassword(event.target.value)} required /></label>{error && <p className={styles.formError} role="alert">{error}</p>}<button className="button" type="submit">Sign in</button></form><p className={styles.authSwitch}>New to ClothMarket? <Link href="/register">Create an account</Link></p></section></main>; }
