"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation"; import { useDemoAuth } from "@/state/useDemoAuth"; import styles from "../auth.module.css";
export default function RegisterPage() { const router = useRouter(); const { signIn } = useDemoAuth(); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (name.trim().length < 2 || !email.includes("@") || password.length < 6) { setError("Add your name, a valid email, and a password of at least 6 characters."); return; } signIn({ name: name.trim(), email }); router.push("/account"); } return <main className={`shell ${styles.authPage}`}><section className={styles.authIntro}><p className="eyebrow">YOUR PERSONAL EDIT</p><h1>Start a smarter <em>style search.</em></h1><p>Create a demo account to save discoveries now; it will connect to secure server authentication in the next backend phase.</p></section><section className={styles.authCard}><h2>Create account</h2><p className={styles.formNote}>This is a local demo session. Do not use a real password.</p><form onSubmit={submit} noValidate><label>Name<input autoComplete="name" value={name} onChange={(event) => setName(event.target.value)} required /></label><label>Email address<input type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label>Password<input type="password" autoComplete="new-password" value={password} onChange={(event) => setPassword(event.target.value)} required /></label>{error && <p className={styles.formError} role="alert">{error}</p>}<button className="button" type="submit">Create demo account</button></form><p className={styles.authSwitch}>Already have an account? <Link href="/login">Sign in</Link></p></section></main>; }
