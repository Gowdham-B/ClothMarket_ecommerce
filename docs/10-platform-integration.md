# Platform integration

Only mock data is used. Platform purchase actions link to platform home pages and explicitly say that the real listing/payment flow is not integrated. Future real providers require contractual/API permission and will implement `PlatformAdapter`; no scraping or unrestricted API assumption is made.
