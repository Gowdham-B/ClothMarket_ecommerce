# Database design

Planned normalized domains: `users`, `products`, `product_variants`, `platforms`, `platform_listings`, `price_history`, `wishlists`, and `comparison_sessions`. Flyway owns schema evolution. No demo frontend data is presented as live retailer data.
